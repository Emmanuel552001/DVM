{% extends 'base.html.twig' %}
 
{% block main %}
    <section>
 
    <h2>Véhicules</h2>
 
    <nav>
        <ul>
            <li><a href="/equipement/creer">[+] Ajouter</a></li>
            <li><a href="/equipement/tout_supprimer">[x] Tout Supprimer</a></li>
        </ul>
    </nav>
 
    {% if equipements_list is empty %}
 
        <p>Aucun véhicule trouvé.</p>
 
    {% else %}
 
    <table>
        <tr>
            <th style="width: 50px">Id</th>
            <th style="width: 150px">Marque</th>
            <th style="width: 150px">Modèle</th>
            <th style="width: 150px">Conducteur</th>
            <th style="width: 50px">Suppr.</th>
            <th style="width: 50px">Mod.</th>
           
        </tr>
        {% for e in equipements_list %}
            <tr>
               
                <td class="center">{{ e.VeId }}</td>
                <td> {{ e.VeMarque }}</td>
                <td> {{ e.VeModele }}</td>
                <td> {{ e.VeConducteur.CoNom }}</td>
                <td class="center"><img src="{{ asset('images/delete.png') }}" alt="delete"
                    onclick="return equipement_supprimer({{ e.VeId }}, '{{ e.VeMarque }}', '{{ e.VeModele }}');" /></td>
                <td class="center"><img src="{{ asset('images/modify.png') }}" alt="modify"
                    onclick="return equipement_modifier({{ e.VeId }}, '{{ e.VeMarque }}', '{{ e.VeModele }}');" /></td>
               
            </tr>
        {% endfor %}
    </table>
 
    {% endif %}
 
    </section>
{% endblock %}