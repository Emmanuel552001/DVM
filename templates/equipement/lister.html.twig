{% extends 'base.html.twig' %}

{% block main %}
    <section class="container my-5">

        <h2 class="my-4">🛠️ Liste des Équipements</h2>

        <div class="d-flex justify-content-center mb-3">
            <a class="btn btn-success me-2" href="/equipement/ajouter">Ajouter</a>
            <a class="btn btn-danger" href="/equipement/tout_supprimer" onclick="return confirm('Êtes-vous sûr de vouloir supprimer tous les équipements ?');">Tout Supprimer</a>
        </div>

        {% if liste_equipements is empty %}
            <p class="text-muted">Aucun équipement trouvé.</p>
        {% else %}
            <div class="table-responsive">
                <table class="table table-striped table-bordered align-middle">
                    <thead class="table-dark">
                        <tr>
                            <th style="width: 50px">Id</th>
                            <th style="width: 150px">Libellé</th>
                            <th style="width: 150px">Prix</th>
                            <th style="width: 50px">Mod.</th>
                            <th style="width: 50px">Suppr.</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for e in liste_equipements %}
                            <tr>
                                <td class="text-center">{{ e.EqId }}</td>
                                <td>{{ e.EqLibelle }}</td>
                                <td>{{ e.EqPrix }}</td>
                                <td class="text-center">
                                    <a href="{{ path('equipement_modifier', {'id': e.EqId}) }}" class="btn btn-link text-success">Modifier</a>

                                </td>
                                <td class="text-center">
                                    <a href="{{ path('equipement_supprimer', {'id': e.EqId}) }}" class="btn btn-link text-danger" onclick="return confirm('Êtes-vous sûr de vouloir supprimer cet équipement ?');">Supprimer</a>
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        {% endif %}
        
    </section>
{% endblock %}
